comandos = []
variaveis = []

nomeArquivo = input('Nome do arquivo: ')

arquivo = open(nomeArquivo, 'r')

for line in arquivo.readLines():
    # Quebra por espaço
    var palavras = line.split()
    
    # Aqui vão ter que ser seguidas certas regras:
    #   1. 
    #
    #
    #
    #
    for word in palavras:
